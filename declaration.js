const onClickAdd = () =>{
    const textLn = document.getElementById("declaration");
    const text = textLn.value;
    const textLng = text.document;
    console.log("text.document");
    if(console.log("text.document")<=100){
        alert("判断が遅い、生殺与奪の権をなんとやら")
    }

    alert("人生はデバックできないからね")
    window.location.href = './index.html'; // 通常の遷移
        window.open('./index.html');
}
document.getElementById("declaration").addEventListener("click",() => onClickAdd());